/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/Dashboard-Financeiro',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
